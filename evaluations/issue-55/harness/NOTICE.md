Files in `pr63-src/` are verbatim extracts from PR #63 (`git show pr-63:src/marvin/<file>`),
commit range ending at 94fdb397. Not modified. Used here to exercise the real
reconciliation/persistence logic against real CouchDB and a real filesystem,
instead of re-implementing it (and re-introducing new made-up assumptions).
